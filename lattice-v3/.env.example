# Docker Compose overrides for Explorer services
# Copy to lattice-v3/.env and edit as needed

# Explorer database connection string
EXPLORER_DATABASE_URL=postgresql://postgres:password@explorer-db:5432/lattice_explorer

# RPC endpoint used by Explorer (web + indexer)
# For devnet container: http://lattice-node-devnet:8545
# For host node: http://host.docker.internal:8545 (macOS/Windows) or http://172.17.0.1:8545 (Linux)
EXPLORER_RPC_ENDPOINT=http://lattice-node-devnet:8545

